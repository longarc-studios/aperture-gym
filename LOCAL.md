# Open this repo in Grok Build CLI

```bash
git clone https://github.com/longarc-studios/aperture-gym.git
cd aperture-gym
grok inspect
grok
```

Paste PROMPT.txt as the first turn. Or headless:

```bash
grok -p "$(cat PROMPT.txt)"
```
