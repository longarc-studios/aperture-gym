# Aperture

Read GROK.md first. This is a computer-use RL gym, not the Grok Build TUI.

Keep worlds, tools, rewards, and traces exact. Continue the Aperture CLI (`bin/aperture.mjs`, `/harness/$taskId`). Do not build a desktop GUI. Do not upload corpus.

```bash
npm install
npm run aperture -- play civic-library --reference
```
